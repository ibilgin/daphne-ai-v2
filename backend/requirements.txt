# ============================================================
# Sketch to Story — backend dependencies
# Python 3.11 required
# Install: uv pip install -r requirements.txt
# ============================================================

# ---------------------------------------------------------------------------
# MLflow & artifact storage
# ---------------------------------------------------------------------------
mlflow>=2.13,<3.0
boto3>=1.34          # S3/MinIO artifact store

# ---------------------------------------------------------------------------
# Model wrappers
# ---------------------------------------------------------------------------
transformers>=4.40   # BLIP captioner
torch>=2.2           # PyTorch (CPU build; MPS auto-selected on Apple Silicon)
Pillow>=10.0         # Image I/O
accelerate>=0.27     # HuggingFace model loading helpers
requests>=2.31       # Ollama HTTP client

# ---------------------------------------------------------------------------
# Evaluation & metrics
# ---------------------------------------------------------------------------
nltk>=3.8            # METEOR score
bert-score>=0.3.13   # BERTScore (roberta-large)

# ---------------------------------------------------------------------------
# Data
# ---------------------------------------------------------------------------
pandas>=2.0
numpy>=1.26

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest>=8.0
pytest-timeout>=2.3

# ---------------------------------------------------------------------------
# Linting
# ---------------------------------------------------------------------------
ruff>=0.4

# ---------------------------------------------------------------------------
# API (Phase 2 — installed here so tests can import schemas)
# ---------------------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2   # Settings from env vars (config.py)
celery>=5.4
redis>=5.0
prometheus-client>=0.20  # GET /metrics exposition

# ---------------------------------------------------------------------------
# Phase 3 — RAG & Agentic Pipeline
# ---------------------------------------------------------------------------
# ChromaDB (persistent vector store)
chromadb>=0.5

# Sentence embeddings (local, no external API)
sentence-transformers>=3.0

# LangChain ecosystem
langchain>=0.2
langchain-core>=0.2
langchain-community>=0.2  # ChatOllama

# LangGraph stateful agent
langgraph>=0.2

# LangSmith local tracing
langsmith>=0.1

# Safety classifier (Detoxify — local inference)
detoxify>=0.5

# PostgreSQL client for human_review queue
psycopg2-binary>=2.9

# ---------------------------------------------------------------------------
# Phase 4 — Monitoring & Drift Detection
# ---------------------------------------------------------------------------
# Evidently drift reports (pin to 0.4.x to keep JSON schema stable)
evidently>=0.4.16,<0.5

# Additional numpy used by drift reporter (already pulled in by torch/pandas,
# listed explicitly so the version constraint is visible)
# numpy already listed above via data section

# ---------------------------------------------------------------------------
# Phase 5 — Governance & Safety
# ---------------------------------------------------------------------------
# JWT authentication (HS256) — auth.py
PyJWT>=2.8

# HashiCorp Vault client — vault_client.py
hvac>=2.1

# Fairlearn bias audit — bias_audit.py
fairlearn>=0.10

# SQLAlchemy async support + asyncpg driver — audit_log.py
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29

# PyYAML — gate_runner.py reads governance.yaml
pyyaml>=6.0
